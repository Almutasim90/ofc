# برومبت بناء النظام — نظام نقاط البيع والمخزون (بيت الشاي)
### منظم بمنهجية Agile / Sprints — جاهز للاستخدام المباشر

---

## السياق العام (يُقرأ قبل أي Sprint)

ابنِ نظام نقاط بيع (POS) مع إدارة مخزون مبنية على وصفات لسلسلة محل شاي وحلويات ومأكولات، بواقع 10 أفرع قابلة للتوسع مستقبلاً. النظام تطبيق ويب يعمل عبر الإنترنت (Online-First)، بمعمارية نظيفة (Clean Architecture) تسمح لاحقاً بإضافة عملاء إضافيين (Electron/تابلت) دون إعادة هيكلة.

**الستاك التقني الإلزامي:**
- **Backend:** ASP.NET Core Web API (.NET 8/9)، بنية Clean Architecture بأربع طبقات: `Domain` / `Application` / `Infrastructure` / `API`
- **Frontend:** React + TypeScript + **Tailwind CSS**، مع `react-i18next` للترجمة، RTL/LTR ديناميكي
- **قاعدة البيانات:** PostgreSQL مستضافة على Supabase — استخدم فقط الـ connection string عبر EF Core. **لا تستخدم Supabase Auth ولا الـ PostgREST API التلقائي** — كل الهوية والصلاحيات تُبنى داخل ASP.NET Core
- **المصادقة:** JWT مخصص + ASP.NET Core Identity
- **الخط:** Tajawal لكلا اللغتين (عربي/إنجليزي)
- **ORM:** Entity Framework Core مع Migrations

**مبادئ معمارية ثابتة تنطبق على كل Sprint:**
1. كل عملية بيع تُطلق Domain Event (`SaleCompleted`) بدل استدعاء مباشر بين الوحدات — يسمح بإضافة مستمعين لاحقاً (طباعة، KDS، إلخ) دون لمس كود المبيعات
2. الوصفة (Recipe) مرتبطة بـ `(ProductId, BranchId)` وليس بالمنتج فقط — لأن نفس المنتج قد يختلف تركيبه بين الأفرع
3. الوصفة **اختيارية لكل منتج** — منتج بدون أي سطر بجدول `ProductRecipe` يُباع عادي بدون أي خصم من المخزون. هذا يخص المنتجات الطازجة اللي تُحضّر لحظة الطلب (بوري، فلافل) ولا تُخزَّن ككمية جاهزة؛ تتبّع استهلاكها التقريبي (مثلاً كم كيلو طحين يقابل كل 100 بوري مباعة) مؤجَّل لمرحلة ما بعد الـ MVP كتحليل إحصائي على تقارير المبيعات، مو ربط مباشر لحظة البيع
4. كل استعلام بالـ API يُفلتر تلقائياً حسب `BranchId` الخاص بالمستخدم عبر EF Core Global Query Filter، باستثناء المدير العام
5. السعر يُخزَّن وقت البيع داخل `SaleItems` (Snapshot)، وليس كمرجع حي للمنتج
6. حقل `BusinessDate` منفصل عن `CreatedAt` بجدول المبيعات لضبط "اليوم المحاسبي" عند الإغلاق المتأخر

---

## قسم التصميم (Design System)

النظام له واجهتان بصريتان مختلفتان تماماً بالاستخدام والجمهور، فينفّذان كمرحلتين منفصلتين — لكن يشتركان بنفس نظام التوكِنز (الألوان والخطوط) لضمان هوية بصرية موحّدة.

### التوكِنز المشتركة

**الألوان:**
| الاسم | القيمة | الاستخدام |
|---|---|---|
| `ink` | `#17110D` | خلفية أساسية (داكنة) |
| `surface` | `#211812` | خلفية العناصر (بطاقات، أشرطة) |
| `surface2` | `#2B2019` | طبقة ثانية فوق surface (أزرار الكمية، الخلفيات الداخلية) |
| `cream` | `#F7F1E6` | النص الأساسي فوق الخلفيات الداكنة |
| `muted` | `#B4A392` | نص ثانوي / تلميحات |
| `saffron` | `#E3A53C` | اللون الأساسي (أزرار الإجراء، الأسعار، العناصر النشطة) |
| `saffronDim` | `#A9762A` | تدرّج مع saffron بالعناصر البرندية |
| `teal` | `#2F6E68` | حالة "متصل / متوفر" |
| `brick` | `#C0503B` | حالة "غير متصل / تحذير / خطأ" |

**الخطوط:**
- **Cairo** (600/700/800) — للعناوين، الأسعار، الأرقام، أي عنصر يحتاج حضور بصري
- **Tajawal** (400/500/700) — لكل النص العادي بكلا اللغتين (عربي/إنجليزي)، كما هو مطلوب بالمشروع

**قاعدة الاستجابة (Responsive):** موبايل وتابلت-عمودي يُعاملان كنفس الفئة (نمط "سلة كدرج منزلق")، وتابلت-أفقي وكمبيوتر يُعاملان كفئة ثانية (نمط "سلة ثابتة جانبية"). نقطة التحوّل: `lg` (1024px) بمقاييس Tailwind الافتراضية.

---

### المرحلة 1 — تصميم شاشة الكاشير (تُنفَّذ ضمن Sprint 3)

**المرجع الجاهز:** ملف `pos-cashier-tailwind.html` المُسلَّم مسبقاً — طبّق نفس البنية والتوكِنز بالضبط عند بناء مكوّنات React الفعلية، لا تُعِد تصميمه من الصفر.

**البنية:**
1. **شريط تصنيفات عمودي** (يمين الشاشة بالعربي) — أيقونات دائرية/مربعة كبيرة بحجم لمس مريح (لا تقل عن 56px)، مع تسمية نصية تحتها، والتصنيف النشط يتلوّن بـ `saffron`
2. **شبكة منتجات مدعومة بالصور** — كل بطاقة منتج تحتوي صورة حقيقية (مربعة، `object-cover`)، الاسم، والسعر بخط Cairo. الشبكة تتوسّع تلقائياً حسب حجم الشاشة (2 أعمدة موبايل → 5 أعمدة شاشات كبيرة)
3. **السلة:**
   - **كمبيوتر / تابلت أفقي (lg+):** لوحة ثابتة بعرض ثابت (384px) على يسار الشاشة، دائماً ظاهرة
   - **تابلت عمودي / موبايل:** زر عائم أسفل الشاشة يعرض عدد الأصناف والإجمالي، يفتح "درج" (Bottom Sheet) ينزلق من الأسفل عند الضغط — يحافظ على أقصى مساحة ممكنة لشبكة المنتجات المصوّرة
4. **تفاعل التأكيد:** عند إضافة منتج، حلقة لونية خفيفة (`saffron`) تدور حول صورة المنتج لحظياً كتأكيد بصري للإضافة (بدون أي حركة مستمرة أو مزعجة، ومعطّلة تلقائياً لو المستخدم مفعّل "تقليل الحركة" بجهازه)

**معيار القبول:** فتح الشاشة على تابلت بوضع عمودي يُظهر شبكة المنتجات بكامل العرض مع زر سلة عائم؛ تدوير نفس الجهاز لوضع أفقي (أو فتحه على كمبيوتر) يُحوّل السلة تلقائياً للوحة ثابتة بدون أي عملية إعادة تحميل.

---

### المرحلة 2 — تصميم لوحة الأدمن (تُنفَّذ ضمن Sprint 7)

**البنية:** قائمة جانبية (Sidebar) بنمط LexOman + Top Bar، باستخدام نفس توكِنز الألوان والخطوط أعلاه (لا لوحة ألوان مختلفة عن شاشة الكاشير).

1. عناصر القائمة الجانبية تُبنى ديناميكياً من صلاحيات المستخدم الفعلية (تفصيل كامل بـ Sprint 7 الأصلي أدناه)
2. القائمة تنعكس تلقائياً (يمين/يسار) حسب اتجاه اللغة
3. الجداول والتقارير تستخدم `surface` كخلفية بطاقات و`cream`/`muted` للنصوص، بنفس منطق التباين المستخدم بشاشة الكاشير — عشان لو مستخدم يتنقّل بين الشاشتين (مدير فرع = كاشير + أدمن) ما يحس بقفزة بصرية

**معيار القبول:** نفس معيار Sprint 7 الأصلي (أدناه) + تأكيد أن نفس ملف توكِنز الألوان (Tailwind config) مستورد بكلا الواجهتين بدون تكرار أو تعارض بالقيم.

---



**الهدف:** هيكل مشروع جاهز للتطوير، متصل بقاعدة بيانات حقيقية، بدعم لغتين من أول Commit.

**المهام:**
1. أنشئ حل (Solution) بـ ASP.NET Core بأربعة مشاريع: `POS.Domain`, `POS.Application`, `POS.Infrastructure`, `POS.API`
2. أنشئ مشروع React منفصل (Vite + TypeScript)
3. اربط `POS.Infrastructure` بـ Supabase Postgres عبر connection string بمتغيرات بيئة (`appsettings.Development.json` + `.env` بالفرونت)
4. أعد أول EF Core Migration فارغة للتأكد من الاتصال
5. أضف `react-i18next` مع ملفين أساسيين: `locales/ar.json` و `locales/en.json`، ومكوّن `LanguageSwitcher` يبدّل `document.dir` تلقائياً
6. حمّل خط Tajawal وCairo (Google Fonts أو self-hosted) وطبّقهما كخطوط افتراضية بكلا اللغتين
7. أعدّ `tailwind.config` بنفس توكِنز الألوان المحددة بقسم "التصميم" أعلاه (`ink`, `surface`, `saffron`, `teal`, `brick`...) — هذا الملف يُستورد لاحقاً بكل من شاشة الكاشير ولوحة الأدمن دون تكرار
8. أعد `Swagger/OpenAPI` بالـ API من البداية

**معيار القبول:** تشغيل الـ Backend يعرض Swagger شغّال، وتشغيل الـ Frontend يعرض صفحة فارغة تتبدّل بين عربي/إنجليزي (اتجاه ونص) بنجاح.

---

## Sprint 1 — الهوية والصلاحيات (RBAC قابل للتخصيص)

**الهدف:** نظام مستخدمين وصلاحيات مرن — أدوار افتراضية + استثناءات فردية.

**الكيانات (Entities):**
```
Users
  - Id, FullName, Username, PasswordHash, BranchId (nullable للمدير العام)
  - RoleId, PreferredLanguage (ar/en), IsActive, CreatedAt

Roles
  - Id, Name (Cashier / BranchManager / GeneralManager), Description

Permissions
  - Id, Key (e.g. "sales.create", "sales.void", "inventory.adjust",
            "reports.branch.view", "reports.global.view", "users.manage",
            "branches.manage", "closing.configure")
  - Description

RolePermissions
  - RoleId, PermissionId   (الصلاحيات الافتراضية لكل دور)

UserPermissionOverrides
  - UserId, PermissionId, IsGranted (true=منح إضافي, false=سحب صريح)
```

**منطق الصلاحية الفعلية عند تسجيل الدخول:**
`الصلاحيات الفعلية = (صلاحيات الدور) ثم طبّق UserPermissionOverrides فوقها (Grant يضيف، Deny يزيل)`

**المهام:**
1. Migration لكل الجداول أعلاه
2. Seed تلقائي للأدوار الثلاثة والصلاحيات الأساسية عند أول تشغيل
3. Endpoint تسجيل الدخول يُرجع JWT يحمل: UserId, BranchId, قائمة الصلاحيات الفعلية (Claims)
4. `Authorization Policy` مخصصة بـ ASP.NET Core تتحقق من الصلاحية المطلوبة لكل Endpoint (مثال: `[RequirePermission("sales.void")]`)
5. Global Query Filter بـ EF Core: أي Entity له `BranchId` يُفلتر تلقائياً حسب فرع المستخدم الحالي، إلا إذا كانت صلاحيته `reports.global.view` أو دوره General Manager
6. شاشة React لإدارة المستخدمين (قائمة، إنشاء، تعديل دور، تفعيل/تعطيل)
7. شاشة تخصيص صلاحيات فردية لكل مستخدم (Grant/Deny) — تظهر فقط لمن يملك صلاحية `users.manage`

**معيار القبول:** إنشاء مستخدم بدور "كاشير" يمنحه تلقائياً صلاحيات البيع فقط. منحه صلاحية إضافية "sales.void" فردياً يجعله قادراً على الاسترجاع دون تغيير دوره.

---

## Sprint 2 — الأفرع والمنتجات والمخزون الأساسي

**الكيانات:**
```
Branches
  - Id, NameAr, NameEn, Code, IsActive

Products
  - Id, NameAr, NameEn, Category (Tea/Sweet/Food), Price, IconOrImageUrl, IsActive

RawMaterials
  - Id, NameAr, NameEn, Unit (piece/kg/liter)

BranchRawMaterialStock
  - BranchId, RawMaterialId, CurrentQuantity, LowStockThreshold

ProductRecipe   (اختياري — منتج بدون سطر هنا يُباع بدون أي خصم من المخزون)
  - ProductId, BranchId, RawMaterialId, QuantityRequired
  (نفس المنتج قد يملك كمية مختلفة لنفس المكوّن حسب الفرع)

StockAdjustments   (سجل تدقيق لأي تعديل يدوي)
  - Id, BranchId, RawMaterialId, QuantityChange, Reason, AdjustedByUserId, AdjustedAt
```

**المهام:**
1. Migrations + CRUD APIs للأفرع والمنتجات والمواد الخام (صلاحية `branches.manage` / `products.manage`)
2. شاشة إدارة الوصفة: لكل منتج، حدد لكل فرع أي مواد خام تُستهلك وبأي كمية — واجعل هذي الخطوة **اختيارية بالكامل** (منتج بدون وصفة = يُحفظ ويُباع عادي بدون أي تتبّع مخزون، وهذا هو الوضع الافتراضي للمنتجات الطازجة مثل بوري والفلافل)
3. Endpoint تعديل مخزون يدوي (توريد بضاعة) يسجّل بـ `StockAdjustments` تلقائياً مع هوية المستخدم والوقت
4. Endpoint يُرجع حالة المخزون الحالية لكل مادة خام بفرع معيّن، مع علم `IsLowStock` بناءً على `LowStockThreshold` الخاص بكل مكوّن ولكل فرع (مو رقم عام)
5. واجهة React لإدارة المنتجات والوصفات والمخزون (خلف صلاحيات مناسبة)

**معيار القبول:** إضافة منتج "شاي كرك" بوصفة تستهلك كوب + كيس شاي، وإضافة منتج "بوري" **بدون أي وصفة** — كلاهما يُحفظ وبيُباع بنجاح، لكن بيع بوري لا يخصم أي شي من `RawMaterials`.

---

## Sprint 3 — شاشة الكاشير (المبيعات)

**الكيانات:**
```
Sales
  - Id, BranchId, ShiftId, CashierUserId, BusinessDate, CreatedAt,
    TotalAmount, PaymentMethod, Status (Completed/Voided)

SaleItems
  - Id, SaleId, ProductId, ProductNameSnapshot, UnitPriceSnapshot,
    Quantity, LineTotal
```

**المهام:**
1. Migrations + Endpoint إنشاء عملية بيع كاملة (Transaction واحدة: إنشاء Sale + SaleItems + خصم المخزون عبر ProductRecipe)
2. عند الإتمام، أطلق Domain Event `SaleCompleted` (حتى لو ما فيه مستمعين فعليين الآن غير خصم المخزون)
3. تحقق من توفر المخزون **قبل** السماح بإتمام البيع — إن كانت أي مادة خام غير كافية، ارفض العملية بخطأ واضح للواجهة
4. ابنِ واجهة الكاشير بـ React + Tailwind **طبقاً بالضبط لبنية المرحلة 1 بقسم "التصميم" أعلاه** (شريط تصنيفات + شبكة منتجات مصوّرة + سلة سلوكها يختلف حسب حجم الشاشة)، مربوطة بالـ API الفعلي بدل بيانات وهمية
5. دعم إلغاء صنف من السلة **قبل** إتمام البيع (بدون أي تأثير على المخزون لأنه لم يُخصم بعد)
6. الواجهة تدعم اللغتين بالكامل (منتجات، أزرار، رسائل الأخطاء)

**معيار القبول:** كاشير يسجّل بيع "شاي كرك × 2 + بوري"، فيُخصم المخزون تلقائياً للشاي فقط (2 كوب + 2 كيس شاي) بمعاملة واحدة ذرية (Atomic)، بينما بوري يُضاف للفاتورة ويُحتسب بالإجمالي بدون أي خصم من `RawMaterials` (لأنه بلا وصفة).

---

## Sprint 4 — الورديات (Shifts)

**الكيانات:**
```
Shifts
  - Id, BranchId, CashierUserId, OpeningCash, ClosingCashExpected,
    ClosingCashActual, VarianceAmount, OpenedAt, ClosedAt,
    Status (Open/Closed), AutoClosed (bool)

VoidRequests   (استرجاع/إلغاء فاتورة بعد الإتمام)
  - Id, SaleId, RequestedByUserId, Reason, ApprovedByUserId, CreatedAt
```

**المهام:**
1. Endpoint فتح وردية: يطلب `OpeningCash`، يربطها بالكاشير والفرع، يمنع فتح وردية ثانية لنفس الكاشير إن كانت له وردية مفتوحة
2. كل عملية بيع تُربط إلزامياً بـ `ShiftId` النشطة للكاشير الحالي
3. Endpoint إغلاق وردية: يحسب `ClosingCashExpected` تلقائياً (الرصيد الافتتاحي + مجموع المبيعات النقدية)، يطلب من الكاشير إدخال `ClosingCashActual`، يحسب `VarianceAmount` تلقائياً
4. بعد الإغلاق، أي فاتورة مرتبطة بهذه الوردية تصبح غير قابلة للتعديل (Immutable)
5. Endpoint طلب استرجاع/إلغاء فاتورة — يتطلب صلاحية `sales.void` وسبب إلزامي، يُسجَّل بجدول `VoidRequests`، ويعيد المخزون المخصوم تلقائياً عبر نفس الوصفة
6. واجهة React لفتح/إغلاق الوردية + شاشة ملخص الوردية (المتوقع مقابل الفعلي)

**معيار القبول:** إغلاق وردية برصيد افتتاحي 50 ر.ع. ومبيعات نقدية 120 ر.ع.، وإدخال عدّ فعلي 168 ر.ع.، يُظهر النظام فرق (Variance) = -2.000 تلقائياً.

---

## Sprint 5 — الإغلاق التلقائي المجدوَل

**الكيانات:**
```
ClosingScheduleConfig
  - Id, DefaultCloseTime (TimeOnly), IsActive

ClosingScheduleExceptions
  - Id, Date, OverrideCloseTime, BranchId (nullable = كل الأفرع), Reason
```

**قرار افتراضي:** `DefaultCloseTime` يُضبط قريب من منتصف الليل بتوقيت مسقط (مثلاً 23:45) — قبل ما يبدأ اليوم المحاسبي الجديد مباشرة، عشان كل مبيعات اليوم تقع ضمن نفس `BusinessDate`. **فتح الوردية بلا أي قيد وقت** — أي كاشير يفتحها بأي ساعة يبدأ فيها دوامه الفعلي (صباحي أو مسائي)، فقط الإغلاق التلقائي مجدوَل.

**المهام:**
1. Endpoint إدارة الإعداد الافتراضي لوقت الإغلاق (صلاحية `closing.configure`، للمدير العام فقط) — القيمة الافتراضية عند أول Seed تكون 23:45
2. Endpoint إدارة الاستثناءات بالتاريخ (لفرع محدد أو لكل الأفرع)
3. Hosted Service (Background Job) بـ ASP.NET Core يعمل كل دقيقة، يتحقق من الوقت الحالي مقابل الجدول الفعّال (استثناء اليوم إن وجد، وإلا الافتراضي)
4. عند الوصول للوقت المحدد ووجود وردية مفتوحة، يغلقها تلقائياً بحالة `AutoClosed = true` و`ClosingCashActual = null` (بانتظار تأكيد المدير لاحقاً)
5. تنبيه للكاشير قبل 15-30 دقيقة من وقت الإغلاق المتوقع (عبر الواجهة، Polling بسيط كافٍ بهذه المرحلة)
6. واجهة React لإدارة جدول الإغلاق والاستثناءات (تقويم بسيط)

**معيار القبول:** ضبط استثناء "عيد الفطر" بوقت إغلاق 01:00 لكل الأفرع، يجعل النظام يتجاهل الوقت الافتراضي لذلك اليوم فقط بكل الأفرع.

---

## Sprint 6 — التقارير

**المهام:**
1. Endpoint تقرير مبيعات يومي لفرع معيّن (إجمالي المبيعات، عدد الفواتير، مقسّم حسب طريقة الدفع) — صلاحية `reports.branch.view`
2. Endpoint تقرير مجمّع لكل الأفرع — صلاحية `reports.global.view` فقط (المدير العام)
3. Endpoint تقرير استهلاك المخزون لكل وردية (أي المواد الخام استُهلكت وبأي كمية)
4. واجهة React لعرض التقارير بجداول + رسوم بيانية بسيطة (مكتبة رسوم خفيفة)، تدعم اللغتين وتصدير CSV كحد أدنى

**معيار القبول:** مدير فرع يرى تقرير فرعه فقط، والمدير العام يرى مقارنة بين كل الأفرع بنفس الشاشة.

---

## Sprint 7 — لوحة الأدمن الكاملة (بنمط LexOman)

**المهام:**
1. ابنِ Layout بقائمة جانبية (Sidebar) + Top Bar، **طبقاً لبنية المرحلة 2 بقسم "التصميم" أعلاه** ونفس توكِنز الألوان/الخطوط المستخدمة بشاشة الكاشير (لا لوحة ألوان منفصلة)
2. عناصر القائمة الجانبية تُبنى ديناميكياً من صلاحيات المستخدم الفعلية (لا تُظهر رابط "المستخدمين" لمن لا يملك `users.manage`، وهكذا)
3. القائمة تنعكس تلقائياً (يمين/يسار) حسب اتجاه اللغة الحالية
4. اجمع كل شاشات الأدمن (المستخدمين، الأفرع، المنتجات، الإغلاق التلقائي، التقارير) تحت هذا الـ Layout الموحّد
5. شاشة إعدادات شخصية للمستخدم: تغيير اللغة المفضّلة، تغيير كلمة المرور

**معيار القبول:** تسجيل دخول بحساب كاشير يعرض قائمة جانبية شبه فارغة (يشوف فقط شاشة البيع)، بينما حساب مدير عام يعرض كل العناصر.

---

## Sprint 8 — الصقل والتحضير للتوسع المستقبلي

**المهام:**
1. اختبارات شاملة (Integration Tests كحد أدنى) على: عزل البيانات بين الأفرع، حساب الصلاحيات الفعلية (دور + استثناءات)، خصم/إرجاع المخزون، حساب فروقات الوردية
2. تأكد أن كل نقاط التوسع المتفق عليها **لا تحتاج تعديل بالكود الحالي** لاحقاً:
   - إضافة طباعة الفاتورة = مستمع جديد على `SaleCompleted`
   - إضافة KDS = مستمع جديد آخر على نفس الحدث
   - إضافة عميل Electron/Capacitor = يستهلك نفس الـ API الحالي بدون تعديل Backend
3. وثّق كل الـ Endpoints عبر Swagger بوصف واضح لكل صلاحية مطلوبة
4. راجع أداء الاستعلامات المرتبطة بالتقارير المجمّعة عبر 10 أفرع (Indexes مناسبة على `BranchId`, `BusinessDate`)

**معيار القبول:** النظام جاهز للنشر التجريبي بفرع واحد حقيقي، مع خطة واضحة موثقة للتوسع لباقي الأفرع.

---

## ملاحظة ختامية للمنفّذ (سواء إنسان أو أداة توليد كود)

نفّذ السبرنتات بالترتيب أعلاه دون تخطي — كل سبرنت يبني على كيانات السبرنت الذي قبله. لا تُدخل أي ميزة من قائمة "Good-to-Have" (خصومات، ولاء عملاء، طباعة، KDS) قبل إتمام كل الـ Must-Have أعلاه بالكامل.
