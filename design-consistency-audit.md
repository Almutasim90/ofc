# تقرير تدقيق وتوحيد التصميم

## النطاق والنتيجة

- تم تدقيق واجهة تسجيل الدخول، الكاشير، صفحات الإدارة، الإعدادات، الورديات، التقارير والإشعارات.
- لم تتغير أي قيمة من قيم ألوان الهوية المعرفة في التوكِنز. استُبدلت الاستخدامات المباشرة داخل الواجهة بأقرب توكِن موجود فقط.
- لم يُعدّل أي منطق أعمال أو استدعاء API ضمن هذا التدقيق.

## ملخص الانحرافات

> الأعداد تمثل مواضع الكود المكتشفة والمصححة بعد تجميع التكرارات المشتركة. قد يخضع الموضع الواحد لأكثر من فئة، مثل زر احتاج تصحيح الحجم والتباعد معاً.

| الفئة | المواضع المكتشفة | النتيجة |
|---|---:|---|
| 1. التباعد | 67 | توحيد الحشو والفجوات على سلم `4/8/12/16/24/32/48px` ونقل التباعد المتكرر إلى الحاويات الأب قدر الإمكان |
| 2. الأيقونات | 38 | توحيد أيقونات الواجهة على `16/20/24px` و`stroke-width: 1.75` واستبدال الرموز النصية الزخرفية بأيقونات Outline |
| 3. الحواف الدائرية | 24 | ربط العناصر الصغيرة/التحكم/الكروت/النوافذ بقيم `8/12/16/24px` |
| 4. الكروت | 15 | توحيد الحشو والحدود والظل وحالات hover/active والاقتطاع للنص الطويل |
| 5. مناطق اللمس | 23 | رفع عناصر الإدارة إلى `44×44px` وعناصر الكاشير الأساسية إلى `56×56px` |
| 6. استخدام الألوان | 18 | إزالة الألوان المباشرة من مكونات الواجهة وربطها بالتوكِنز الحالية من دون تغيير قيم التوكِنز |
| 7. الهوية والاتساق | 10 | توحيد Cairo للعناوين والقيم البارزة وTajawal للنصوص، وتوحيد اسم وشارة «لولاة السويق» والمصطلحات |

## الملفات والمكونات المعدلة

### الأساس والمكونات المشتركة

- `frontend/src/index.css`
- `frontend/src/components/AppIcon.tsx`
- `frontend/src/components/Layout.tsx`
- `frontend/src/components/BottomSheet.tsx`
- `frontend/src/components/NotificationBell.tsx`
- `frontend/src/components/PasswordField.tsx`
- `frontend/src/components/TableTools.tsx`
- `frontend/src/components/ThemeToggle.tsx`
- `frontend/src/i18n.ts`
- `frontend/src/components/LanguageSwitcher.tsx`

### الصفحات

- `AiSettingsPage.tsx`
- `BranchesPage.tsx`
- `CashierPage.tsx`
- `ClosingSchedulePage.tsx`
- `InventoryPage.tsx`
- `NotificationsPage.tsx`
- `ProductsPage.tsx`
- `RawMaterialsPage.tsx`
- `ReportsPage.tsx`
- `SettingsPage.tsx`
- `ShiftPage.tsx`
- `UserPermissionsPage.tsx`
- `UsersPage.tsx`

### النصوص والهوية

- `frontend/src/locales/ar.json`
- `frontend/src/locales/en.json`

## التحقق

- نجح بناء الإنتاج عبر `npm run build` دون أخطاء TypeScript أو Vite.
- تم فحص التخطيط بصرياً على سطح المكتب والهاتف، وفي `RTL/LTR` والوضعين الفاتح والداكن، دون تمرير أفقي في الصفحات التي تم فتحها.
- بقيت ألوان أعلام مبدّل اللغة مكتوبة داخل رسم SVG عمداً لأنها ألوان أصلية لعلم وليست ألوان واجهة قابلة للتبديل.
- بقيت سماكات خطوط الرسوم البيانية مستقلة عن قاعدة الأيقونات لأنها تمثل سلاسل بيانات وليست أيقونات واجهة.

## قرارات بشرية مطلوبة

لا يوجد انحراف بصري كبير متبقٍ يحتاج افتراضاً أو قراراً بشرياً قبل التصحيح.
