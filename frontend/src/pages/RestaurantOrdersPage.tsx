import { useEffect,useMemo,useState } from 'react';import { useTranslation } from 'react-i18next';import { api,ApiError } from '../api/client';import type{BranchDto,ComboComponentDto,MenuCategoryDto,MenuItemDto,ModifierGroupDto,OrderTypeDto,RestaurantOrderDto,RestaurantTableDto}from'../api/types';import Money from'../components/Money';import{useToast}from'../components/ToastContext';
type Draft={item:MenuItemDto;quantity:number;modifierOptionIds:string[];comboSelections:{comboComponentId:string;selectedMenuItemId:string}[];delta:number};
export default function RestaurantOrdersPage(){const{t,i18n}=useTranslation();const toast=useToast();const[branches,setBranches]=useState<BranchDto[]>([]),[branchId,setBranchId]=useState(''),[types,setTypes]=useState<OrderTypeDto[]>([]),[typeId,setTypeId]=useState(''),[tables,setTables]=useState<RestaurantTableDto[]>([]),[tableId,setTableId]=useState(''),[cats,setCats]=useState<MenuCategoryDto[]>([]),[items,setItems]=useState<MenuItemDto[]>([]),[cart,setCart]=useState<Draft[]>([]),[config,setConfig]=useState<{item:MenuItemDto;groups:ModifierGroupDto[];slots:ComboComponentDto[];selected:string[]}|null>(null),[orders,setOrders]=useState<RestaurantOrderDto[]>([]);const name=(x:{nameAr:string;nameEn:string})=>i18n.language==='ar'?x.nameAr:x.nameEn;
useEffect(()=>{void(async()=>{const[b,ty,it]=await Promise.all([api.get<BranchDto[]>('/api/branches'),api.get<OrderTypeDto[]>('/api/restaurant-orders/types'),api.get<MenuItemDto[]>('/api/restaurant-catalog/items')]);setBranches(b);setBranchId(b[0]?.id??'');setTypes(ty);setTypeId(ty[0]?.id??'');setItems(it)})()},[]);useEffect(()=>{if(!branchId)return;void Promise.all([api.get<RestaurantTableDto[]>(`/api/restaurant-catalog/tables?branchId=${branchId}`),api.get<MenuCategoryDto[]>(`/api/restaurant-catalog/categories?branchId=${branchId}`),api.get<RestaurantOrderDto[]>(`/api/restaurant-orders?branchId=${branchId}`)]).then(([a,b,c])=>{setTables(a.filter(x=>x.isActive));setCats(b.filter(x=>x.isActive&&x.isAvailable));setOrders(c)})},[branchId]);const visible=useMemo(()=>items.filter(x=>x.isActive&&cats.some(c=>c.id===x.categoryId)),[items,cats]);const selectedType=types.find(x=>x.id===typeId);
const choose=async(item:MenuItemDto)=>{try{const groups=item.kind==='SingleProduct'?await api.get<ModifierGroupDto[]>(`/api/modifiers?menuItemId=${item.id}`):[];const slots=item.kind==='Combo'?await api.get<ComboComponentDto[]>(`/api/restaurant-catalog/combos/${item.id}`):[];if(!groups.length&&!slots.length){setCart(x=>[...x,{item,quantity:1,modifierOptionIds:[],comboSelections:[],delta:0}]);return}setConfig({item,groups,slots,selected:[]})}catch(e){toast.error(e instanceof ApiError?e.message:t('common.saveError'))}};
const confirm=()=>{if(!config)return;for(const g of config.groups){const n=g.options.filter(o=>config.selected.includes(o.id)).length,min=g.isRequired?Math.max(1,g.minSelect):g.minSelect;if(n<min||n>g.maxSelect){toast.error(`${name(g)}: ${min}-${g.maxSelect}`);return}}for(const s of config.slots){const n=s.options.filter(o=>config.selected.includes(o.id)).length,min=s.isRequired?Math.max(1,s.minSelect):s.minSelect;if(n<min||n>s.maxSelect){toast.error(`${s.slotLabel}: ${min}-${s.maxSelect}`);return}}const mods=config.groups.flatMap(g=>g.options).filter(o=>config.selected.includes(o.id));const combos=config.slots.flatMap(s=>s.options.filter(o=>config.selected.includes(o.id)).map(o=>({comboComponentId:s.id,selectedMenuItemId:o.menuItemId})));setCart(x=>[...x,{item:config.item,quantity:1,modifierOptionIds:mods.map(x=>x.id),comboSelections:combos,delta:mods.reduce((a,b)=>a+b.priceDelta,0)+config.slots.flatMap(s=>s.options).filter(o=>config.selected.includes(o.id)).reduce((a,b)=>a+b.priceDelta,0)}]);setConfig(null)};
const submit=async()=>{try{if(selectedType?.code==='DINE_IN'&&!tableId){toast.error(t('restaurantOrders.tableRequired'));return}const order=await api.post<RestaurantOrderDto>('/api/restaurant-orders',{branchId,orderTypeId:typeId,tableId:selectedType?.code==='DINE_IN'?tableId:null,carPlateNumber:null,discountAmount:0,lines:cart.map(x=>({menuItemId:x.item.id,quantity:x.quantity,notes:null,modifierOptionIds:x.modifierOptionIds,comboSelections:x.comboSelections}))});setCart([]);setOrders(x=>[order,...x]);toast.success(t('restaurantOrders.created'))}catch(e){toast.error(e instanceof ApiError?e.message:t('common.saveError'))}};const total=cart.reduce((a,x)=>a+(x.item.basePrice+x.delta)*x.quantity,0);
return <section><h1>{t('restaurantOrders.title')}</h1><div className="table-toolbar"><select value={branchId} onChange={e=>setBranchId(e.target.value)}>{branches.map(x=><option key={x.id} value={x.id}>{name(x)}</option>)}</select><select value={typeId} onChange={e=>setTypeId(e.target.value)}>{types.map(x=><option key={x.id} value={x.id}>{name(x)}</option>)}</select>{selectedType?.code==='DINE_IN'&&<select value={tableId} onChange={e=>setTableId(e.target.value)}><option value="">{t('restaurantOrders.selectTable')}</option>{tables.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select>}</div><div className="grid gap-6 lg:grid-cols-[1fr_22rem]"><div className="cashier-products-grid">{visible.map(x=><button className="product-card p-4" key={x.id} onClick={()=>choose(x)}><strong>{name(x)}</strong><Money value={x.basePrice}/></button>)}</div><aside className="settings-card"><h2>{t('cashier.cart')}</h2>{cart.map((x,i)=><div className="table-toolbar" key={i}><span>{name(x.item)}</span><Money value={(x.item.basePrice+x.delta)*x.quantity}/><button onClick={()=>setCart(c=>c.filter((_,n)=>n!==i))}>×</button></div>)}<strong><Money value={total}/></strong><button disabled={!cart.length} onClick={submit}>{t('restaurantOrders.create')}</button></aside></div><h2>{t('restaurantOrders.recent')}</h2>{orders.slice(0,10).map(x=><div className="table-toolbar" key={x.id}><span>#{x.orderNumber} · {x.orderTypeCode}{x.tableLabel?` · ${x.tableLabel}`:''}</span><Money value={x.grandTotal}/></div>)}
{config&&<div className="app-scrim fixed inset-0 z-50 flex items-center justify-center"><div className="settings-card max-h-[85vh] w-[min(42rem,94vw)] overflow-auto"><h2>{name(config.item)}</h2>{config.groups.map(g=><fieldset key={g.id}><legend>{name(g)} ({g.isRequired?t('restaurant.required'):t('modifiers.minimum')})</legend>{g.options.filter(o=>o.isActive).map(o=><label className="checkbox-row" key={o.id}><input type={g.maxSelect===1?'radio':'checkbox'} name={g.id} checked={config.selected.includes(o.id)} onChange={()=>setConfig(c=>c&&({...c,selected:g.maxSelect===1?[...c.selected.filter(id=>!g.options.some(o=>o.id===id)),o.id]:c.selected.includes(o.id)?c.selected.filter(id=>id!==o.id):[...c.selected,o.id]}))}/>{name(o)} <Money value={o.priceDelta}/></label>)}</fieldset>)}{config.slots.map(s=><fieldset key={s.id}><legend>{s.slotLabel}</legend>{s.options.map(o=><label className="checkbox-row" key={o.id}><input type={s.maxSelect===1?'radio':'checkbox'} name={s.id} checked={config.selected.includes(o.id)} onChange={()=>setConfig(c=>c&&({...c,selected:s.maxSelect===1?[...c.selected.filter(id=>!s.options.some(o=>o.id===id)),o.id]:c.selected.includes(o.id)?c.selected.filter(id=>id!==o.id):[...c.selected,o.id]}))}/>{name({nameAr:o.menuItemNameAr,nameEn:o.menuItemNameEn})} <Money value={o.priceDelta}/></label>)}</fieldset>)}<div className="modal-actions"><button onClick={()=>setConfig(null)}>{t('common.cancel')}</button><button onClick={confirm}>{t('orders.add')}</button></div></div></div>}</section>}
