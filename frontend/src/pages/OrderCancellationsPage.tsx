import{useCallback,useEffect,useState}from'react';import{useTranslation}from'react-i18next';import{api,ApiError}from'../api/client';import type{BranchDto,OrderCancellationDto,RestaurantOrderDto}from'../api/types';import Money from'../components/Money';import{useToast}from'../components/ToastContext';
export default function OrderCancellationsPage(){const{t}=useTranslation();const toast=useToast();const[branches,setBranches]=useState<BranchDto[]>([]),[branchId,setBranchId]=useState(''),[orders,setOrders]=useState<RestaurantOrderDto[]>([]),[log,setLog]=useState<OrderCancellationDto[]>([]);useEffect(()=>{void api.get<BranchDto[]>('/api/branches').then(x=>{setBranches(x);setBranchId(x[0]?.id??'')})},[]);const load=useCallback(async()=>{if(!branchId)return;const[o,l]=await Promise.all([api.get<RestaurantOrderDto[]>(`/api/restaurant-orders?branchId=${branchId}`),api.get<OrderCancellationDto[]>(`/api/restaurant-orders/cancellations?branchId=${branchId}`)]);setOrders(o);setLog(l)},[branchId]);useEffect(()=>{void load()},[load]);const cancel=async(orderId:string,itemId?:string)=>{const reason=window.prompt(t('cancellations.reason'));if(!reason)return;try{await api.post(`/api/restaurant-orders/${orderId}${itemId?`/items/${itemId}`:''}/cancel`,{reason});await load();toast.success(t('cancellations.saved'))}catch(e){toast.error(e instanceof ApiError?e.message:t('common.saveError'))}};
return<section><h1>{t('cancellations.title')}</h1><label>{t('restaurant.branch')}<select value={branchId} onChange={e=>setBranchId(e.target.value)}>{branches.map(x=><option value={x.id} key={x.id}>{x.nameAr} / {x.nameEn}</option>)}</select></label><h2>{t('cancellations.openOrders')}</h2>{orders.filter(x=>!['Paid','Closed','Cancelled'].includes(x.status)).map(o=><div className="settings-card" key={o.id}><div className="table-toolbar"><strong>#{o.orderNumber}</strong><Money value={o.grandTotal}/><button onClick={()=>cancel(o.id)}>{t('cancellations.cancelOrder')}</button></div>{o.items.filter(x=>!x.isCancelled).map(i=><div className="table-toolbar" key={i.id}><span>{i.name} × {i.quantity}</span><Money value={i.lineTotal}/><button onClick={()=>cancel(o.id,i.id)}>{t('cancellations.cancelItem')}</button></div>)}</div>)}<h2>{t('cancellations.log')}</h2><div className="table-shell"><table><thead><tr><th>#</th><th>{t('cancellations.item')}</th><th>{t('cancellations.reason')}</th><th>{t('cancellations.date')}</th></tr></thead><tbody>{log.map(x=><tr key={x.id}><td>{x.orderNumber}</td><td>{x.itemName??t('cancellations.wholeOrder')}</td><td>{x.reason}</td><td>{new Date(x.createdAt).toLocaleString()}</td></tr>)}</tbody></table></div></section>}
