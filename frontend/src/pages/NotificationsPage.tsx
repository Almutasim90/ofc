import { useEffect, useState } from 'react';import { useTranslation } from 'react-i18next';import { api } from '../api/client'
interface Alert{id:string;branchNameAr:string;branchNameEn:string;materialNameAr:string;materialNameEn:string;triggeredAt:string;resolvedAt:string|null}
export default function NotificationsPage(){const{t,i18n}=useTranslation();const[rows,setRows]=useState<Alert[]>([]);useEffect(()=>{api.get<Alert[]>('/api/notifications/low-stock?includeResolved=true').then(setRows)},[]);return <section><h1>{t('notifications.history')}</h1><div className="table-shell"><table><tbody>{rows.map(x=><tr key={x.id}><td>{i18n.language==='ar'?x.materialNameAr:x.materialNameEn}</td><td>{i18n.language==='ar'?x.branchNameAr:x.branchNameEn}</td><td>{new Date(x.triggeredAt).toLocaleString(i18n.language)}</td><td>{x.resolvedAt?t('notifications.resolved'):t('notifications.active')}</td></tr>)}</tbody></table></div></section>}
