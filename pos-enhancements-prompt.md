# برومبت التحسينات — إضافات فوق نظام منفَّذ (حتى Sprint 7)
### للاستخدام المباشر مع أداة التنفيذ (Codex)

---

## ⚠️ قيد صارم غير قابل للتفاوض

**ممنوع لمس أو تعديل أي كود متعلق بالألوان** — Tailwind tokens، متغيرات CSS، ملفات الثيم، أي قيمة Hex/RGB موجودة حالياً بالمشروع. استخدم التوكِنز الموجودة حرفياً كما هي (بأسمائها الحالية بالكود) بأي كومبوننت جديد تبنيه بهذا البرومبت. **لا تُنشئ، لا تُعيد تسمية، ولا تعدّل أي تعريف لون.** تحسين لوحة الألوان مؤجَّل لطلب منفصل لاحقاً بقرار من العميل.

---

## السياق (لا يُنفَّذ — للفهم فقط)

النظام منفَّذ فعلياً حتى Sprint 7: الهوية والصلاحيات (RBAC)، الأفرع والمنتجات والمخزون الأساسي (Recipe اختيارية، `TrackingMode`)، شاشة الكاشير (شبكة منتجات + سلة تكيّفية)، الورديات (فتح بضغطة واحدة + عدّ فئات نقدية بالإغلاق)، الإغلاق التلقائي المجدوَل، التقارير الأساسية، ولوحة الأدمن. **هذا البرومبت إضافي بحت** — كل مهمة أدناه تفترض الجداول والكومبوننتات الموجودة، وتتعامل معها بـ Migrations إضافية (`ALTER`/جداول جديدة)، لا بإعادة إنشاء أي شي من الصفر.

---

## مبادئ إضافية تنطبق على كل ميزة بهذا البرومبت

1. **الدقة العشرية:** أي حقل كمية أو مبلغ جديد يُضاف = `decimal` بدقة ثابتة (`decimal(18,3)`)، بلا استثناء
2. **حماية التعارض:** أي تعديل جديد على `BranchRawMaterialStock` (مباشر أو غير مباشر) يجب يمر بنفس آلية الحماية من التعارض المستخدمة أصلاً بمنطق خصم المخزون الحالي — لا تضف مسار تحديث ثانٍ بدون نفس الحماية
3. **إعادة استخدام المكوّنات الموجودة أولى من بناء جديد** — مثال: أي درج منزلق جديد يُبنى = نفس مكوّن Bottom Sheet المستخدم أصلاً بسلة الكاشير، لا مكوّن جديد
4. **كل نص جديد بكلا اللغتين** (`ar.json` / `en.json`) من نفس Commit، لا لاحقاً

---

## الميزة 1 — الخصومات (قيمة ثابتة + نسبة)

**تعديلات على الجداول الموجودة (Migration جديد، لا إعادة إنشاء):**
```
ALTER TABLE Sales ADD:
  DiscountType (None/Percentage/FixedAmount), DiscountValue (decimal),
  DiscountAmount (decimal — المحسوب فعلياً بعد التطبيق)

ALTER TABLE SaleItems ADD:
  DiscountType (None/Percentage/FixedAmount), DiscountValue (decimal)
  -- LineTotal الموجود يصير بعد تطبيق خصم السطر إن وُجد
```

**المهام:**
1. Migration للحقول أعلاه على الجداول الموجودة
2. منطق التطبيق: خصم على مستوى الفاتورة كاملة (`Sales`) أو على صنف معيّن بالسلة (`SaleItems`) — **بدون أي قيد صلاحية**، أي كاشير يقدر يطبّقه مباشرة
3. أضف بواجهة الكاشير الموجودة عنصر تحكم الخصم (نسبة/قيمة) — إما بسطر كل صنف بالسلة، أو بذيل ملخص الفاتورة للخصم الكلي؛ استخدم نفس بطاقات/أزرار السلة الموجودة، لا تصميم بصري جديد
4. حدّث حساب `TotalAmount` بالفاتورة ليعكس `DiscountAmount` تلقائياً
5. **Endpoint تقرير الخصومات (جديد):** إجمالي الخصومات الممنوحة لكل فرع/كاشير/فترة، مع تفصيل كل فاتورة فيها خصم — هذا هو آلية الرقابة الوحيدة بما إن الخصم بلا قيد صلاحية
6. أضف بطاقة "إجمالي الخصومات اليوم" لبطاقات KPI الموجودة بشاشة التقارير

**معيار القبول:** تطبيق خصم 10% على فاتورة إجمالها 5.000 ر.ع. يُظهر `DiscountAmount = 0.500` والإجمالي النهائي `4.500` تلقائياً، ويظهر بتقرير الخصومات الجديد فوراً.

---

## الميزة 2 — قنوات البيع الإلكترونية (تطبيقات التوصيل)

**جداول جديدة:**
```
SalesChannels
  - Id, NameAr, NameEn, LogoUrl, IsActive, IsInStore (bool)
  -- قناة "المحل" تُزرع تلقائياً بـ Seed جديد بعلم IsInStore=true، لا تُحذف
  -- بقية القنوات (تطبيقات التوصيل) تُضاف من لوحة الأدمن، قابلة للتوسع بلا حدود

ProductChannelPrice   (اختياري — بدون سطر هنا، القناة تستخدم Products.Price العادي)
  - ProductId, ChannelId, Price
```

**تعديل على الجدول الموجود:**
```
ALTER TABLE Sales ADD: ChannelId (افتراضياً = قناة "المحل")
```

**المهام:**
1. Migrations للجداول أعلاه + Seed قناة "المحل"
2. CRUD إدارة القنوات بلوحة الأدمن الموجودة (صلاحية جديدة `channels.manage`، أضفها لجدول `Permissions` الموجود، المدير العام فقط)
3. شاشة تسعير لكل قناة: لكل منتج موجود بالكتالوج، سعر اختياري مخصص لتلك القناة
4. **بشاشة الكاشير الموجودة:** زر "طلبات إلكترونية" بأعلى الشاشة (بجانب شريط التصنيفات الموجود، لا داخله) يعرض شبكة صغيرة بشعارات القنوات النشطة؛ اختيار قناة يبدّل الأسعار المعروضة بشبكة المنتجات الموجودة لتعكس `ProductChannelPrice`، مع شريط واضح "الطلب الحالي: [شعار القناة]"، وزر "رجوع للمحل" يلغي السياق
5. **تعديل حساب النقد المتوقع بإغلاق الوردية (الموجود):** استثنِ صراحة أي `Sale` بـ `ChannelId` غير `IsInStore` من مجموع المبيعات النقدية — الدفع يستلمه تطبيق التوصيل لا الكاشير
6. **Endpoint تقرير توزيع القنوات (جديد):** مبيعات كل قناة بفترة معيّنة، لمطابقة تسويات التطبيقات لاحقاً

**معيار القبول:** إضافة قناة توصيل جديدة وتسعير منتج موجود بسعر مختلف لها — بيع عبر تلك القناة يستخدم السعر المخصص، بيع بالمحل بنفس اللحظة يستخدم السعر العادي. إغلاق وردية فيها بيع بقناة توصيل لا يُدرجه بحساب النقد المتوقع.

---

## الميزة 3 — تنبيهات المخزون المنخفض (إشعار + بريد إلكتروني)

**جدول جديد:**
```
LowStockNotifications
  - Id, BranchId, RawMaterialId, TriggeredAt, ResolvedAt (nullable)
```

**المهام:**
1. Migration للجدول أعلاه
2. Background Job (Hosted Service) يفحص جدول المخزون الموجود (`BranchRawMaterialStock`) مقابل `LowStockThreshold` الموجود أصلاً
3. عند أول عبور تحت الحد: أنشئ سجل تنبيه، أرسل **إشعار داخل النظام** لمدير الفرع المعني + المدير العام، وأرسل **بريد إلكتروني** عبر خدمة SMTP مستقلة جديدة (منفصلة تماماً عن أي إعداد Supabase — غير مستخدم بهذا المشروع أصلاً)
4. **لا تكرار:** لا إشعار ثانٍ لنفس المادة/الفرع طالما `ResolvedAt` فاضي؛ يُغلق تلقائياً لما الرصيد يرجع فوق الحد (عبر تعديل مخزون يدوي موجود أصلاً)، وأي هبوط جديد لاحقاً يُطلق إشعار من الصفر
5. جرس إشعارات جديد بأعلى لوحة الأدمن الموجودة (يتبع نفس منطق الظهور حسب الصلاحيات الحالي) + شاشة سجل تنبيهات

**معيار القبول:** مادة خام تهبط تحت حدها → إشعار وبريد فوري لمدير الفرع والمدير العام. بيع إضافي وهي لسه تحت الحد لا يرسل إشعار ثانٍ. توريد بضاعة يرجّع الرصيد فوق الحد → التنبيه يُغلق تلقائياً.

---

## الميزة 4 — التحليل الذكي (AI-Powered Insights)

**نطاق محدد:** ملخص نصي لأداء المبيعات + اكتشاف شذوذ (نقص غير طبيعي، فروقات ورديات متكررة) + توقع طلب مستقبلي تقريبي. **عند الطلب اليدوي فقط** (زر) — لا استدعاء تلقائي مستمر.

**جداول جديدة:**
```
AiProviderSettings   (إعداد واحد فعّال بأي وقت، المدير العام فقط يعدّله)
  - Id, Provider (OpenAI/Anthropic/...), Model, ApiKeyEncrypted, IsActive

AiInsightRequests   (سجل تدقيق لمراقبة التكلفة)
  - Id, RequestedByUserId, BranchId (nullable = كل الأفرع),
    RequestType (Summary/AnomalyDetection/DemandForecast), CreatedAt, ResultSummary
```

**المهام:**
1. Migrations للجداول أعلاه
2. شاشة إعدادات "الذكاء الاصطناعي" جديدة بلوحة الأدمن — اختيار المزوّد والنموذج، إدخال مفتاح API **يُشفَّر فوراً عبر ASP.NET Core Data Protection API قبل الحفظ** (ممنوع تخزينه كنص عادي)، ولا يُعرض كامل بعد الحفظ (آخر 4 خانات فقط للتأكيد)
3. Endpoint توليد ملخص نصي لأداء فرع/كل الأفرع، مبني على بيانات التقارير الفعلية الموجودة (لا بيانات وهمية)
4. Endpoint اكتشاف شذوذ: يقارن معدلات الاستهلاك والفروقات الأخيرة (من جداول الورديات والمخزون الموجودة) بمتوسطها التاريخي
5. Endpoint توقع طلب تقريبي: تقدير استرشادي واضح المصدر بناءً على تاريخ المبيعات الموجود، مو رقم قاطع
6. زر "تحليل ذكي" جديد بأعلى شاشة التقارير الموجودة، مع مؤشر تحميل واضح أثناء الانتظار

**معيار القبول:** المدير العام يضبط مزوّد AI ونموذج ومفتاح API (يظهر مشفّراً بقاعدة البيانات). الضغط على "تحليل ذكي" بالتقارير يولّد ملخصاً نصياً حقيقياً مبنياً على بيانات فعلية، ويُسجَّل الطلب للتدقيق.

---

## الميزة 5 — إعادة تصميم لوحة الأدمن (هيكل + إعدادات + تقارير) — بدون لمس الألوان

هذي إعادة تصميم **بنيوية فقط** (تخطيط، تنقّل، عرض معلومات) — **صفر تغيير على أي قيمة لون موجودة بالكود** حسب القيد بأعلى هذا المستند.

### 5أ — هيكل تكيّفي بدل التخطيط الحالي

| الوضع | نمط التنقّل |
|---|---|
| كمبيوتر / تابلت أفقي | شريط جانبي قابل للطي — أيقونة+تسمية موسّع، أيقونة فقط مطوي |
| تابلت عمودي / موبايل | شريط تبويبات سفلي بحد أقصى 5 عناصر رئيسية + "المزيد" يفتح **نفس مكوّن الـ Bottom Sheet المستخدم أصلاً بسلة الكاشير** (إعادة استخدام حرفية، لا بناء جديد) |

عناصر التنقّل (بكلا الوضعين) تبقى مبنية ديناميكياً من الصلاحيات الفعلية كما هو مطبَّق حالياً — فقط غلاف العرض يتغيّر.

### 5ب — مركز إعدادات مرن بدل القائمة الحالية

1. شاشة إعدادات واحدة، **بطاقات موضوعية** بدل قائمة نصية طويلة — كل بطاقة = مجموعة إعدادات مترابطة (الإغلاق التلقائي، الصلاحيات، الأفرع، القنوات، الذكاء الاصطناعي)
2. **كل بطاقة تعرض ملخص حالتها الحالية مباشرة على وجهها** (مثال: "الإغلاق التلقائي: 23:45 · استثناء واحد نشط") بدون فتحها
3. بحث واحد أعلى الشاشة يفلتر البطاقات فوراً
4. الظهور مبني على الصلاحيات الفعلية — بطاقة تختفي كاملة لا تظهر معطّلة

### 5ج — تقارير Chart.js بدل الجداول الحالية

1. أضف `chart.js` + `react-chartjs-2` كتبعية جديدة بالفرونت
2. أعد ترتيب شاشة التقارير الموجودة: **بطاقات KPI مختصرة أعلى الصفحة أولاً** (مبيعات اليوم، عدد الفواتير، تنبيهات مخزون منخفض، إجمالي الخصومات) — رقم بارز واحد لكل بطاقة
3. تحتها رسوم بيانية (تستبدل الجداول كواجهة افتراضية، لا تحذفها):
   - مبيعات كل فرع (اليوم/الأسبوع) → Bar Chart أفقي
   - اتجاه المبيعات عبر الوقت → Line Chart بخط واحد افتراضياً (تراكب عدّة خطوط اختياري بفلتر يدوي فقط)
   - المنتجات الأكثر مبيعاً → Horizontal Bar
   - فروقات الورديات عبر الوقت → Bar Chart بلونين تلقائياً حسب زيادة/نقص (بالقيم الموجودة حالياً، لا تعريف لون جديد — استخدم توكِنز `danger`/`primary` أو ما يعادلها الموجود بالكود فعلاً)
   - توزيع قنوات البيع (من الميزة 2) → Donut
4. **تفاعل اللمس لا التحويم:** فعّل `interaction.mode: 'nearest'` مع دعم صريح لأحداث اللمس بإعداد Chart.js — التابلت هو الاستخدام الأساسي
5. الجدول التفصيلي الموجود يبقى، لكن خلف زر "عرض كجدول" اختياري أسفل كل رسم — لا كواجهة افتراضية بعد اليوم

**معيار القبول (الميزة 5 كاملة):**
- فتح لوحة الأدمن على تابلت عمودي يُظهر شريط تبويبات سفلي؛ تدويره لوضع أفقي يحوّله تلقائياً لشريط جانبي، بدون إعادة تحميل، وبدون أي تغيير بألوان الواجهة عن النسخة الحالية
- فتح الإعدادات يعرض بطاقات بملخصات حالة ظاهرة، والبحث يفلترها فوراً
- فتح التقارير يعرض KPI أولاً ثم رسوم Chart.js تفاعلية باللمس، والجدول القديم متاح خلف زر اختياري

---

## ملاحظة ختامية

نفّذ الميزات أعلاه بالترتيب المرقّم (1 → 5) — كل ميزة مستقلة نسبياً عن الأخرى عدا الميزة 5ج اللي تعتمد على بطاقة KPI الخصومات من الميزة 1 وبطاقة توزيع القنوات من الميزة 2، فنفّذهم قبلها. **التذكير الأخير: أي كومبوننت جديد بهذا البرومبت يجب يستخدم توكِنز الألوان الموجودة بالكود حرفياً كما هي — بدون أي استثناء.**
